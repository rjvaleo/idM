/** Effective rendered pixel targets at the 640×480 / 100% baseline. */
export const TYPOGRAPHY = {
  globalMenu: 11,
  title: 10,
  titleBar: 16,
  titleNote: 8,
  body: 8,
  compact: 7,
} as const;
